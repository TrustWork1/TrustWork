[tool.poetry]
name = "escrow-microservice"
version = "0.1.0"
description = ""
authors = ["Swapnil Chopra <swapnil.chopra@webskitters.in>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^4.2"
requests = "^2.32.3"
basicauth = "^1.0.0"
djangorestframework = "^3.15.2"
celery = "^5.4.0"
redis = "^5.1.0"
django-environ = "^0.11.2"
psycopg2-binary = "^2.9.10"
stripe = "^11.4.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
