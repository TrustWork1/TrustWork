import { Box, styled } from '@mui/material';

export const HomePageBox = styled(Box)``;

export const BestPackageBox = styled(Box)`
  position: relative;
  .pricing-list {
    margin-top: 40px;
  }

  .float-shape {
    position: absolute;
    left: 0;
    top: 53%;
    transform: translateY(-50%);
  }
`;
